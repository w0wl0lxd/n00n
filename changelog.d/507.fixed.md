Fixed steering and control interrupts dropping images, including image-only messages, blocked Enter and Tab-to-queue while attachments are still loading, and cleared disconnected loaders with an explicit error. Routed pastes to overlays first and kept pasted text, including image paths, even when image loading fails or is unsupported. Wrapped long option labels and narrow layouts, fixed custom-option scroll focus, removed inactive Tab hints, and sized question dialogs to content on first open.
