Fixed the todo-line shrink binary search stalling forever on multi-byte content (the prior char-boundary snap could still leave the search range stuck), hard-bounded the todo prompt block when hint text alone exceeds the cap, made the Lua todo renderer cut multi-byte content on valid UTF-8 boundaries, clamped the compaction summarizer's context window to the smaller of the compact/main models in both the run and UI compaction paths, recomputed YOLO-gated history replay live each turn instead of at run start, sanitized and count-matched image captions before embedding them in transcripts, stopped `ToolOutput::bounded` from rendering its text three times and from warning once per truncated line, self-healed poisoned Lua plugin-state locks instead of bricking state after one panic, and fixed the Alt+T model-picker thinking cycle and TUI reply grace window.
