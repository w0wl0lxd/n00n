Per-model file input support flags: static model tables carry a `files` flag, discovery can report file support, and overrides still take precedence. Regular OpenAI GPT-5.6 entries are file-capable; coding-plan and Codex entries stay disabled.
