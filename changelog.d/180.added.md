Added beta Fusion dual-lane routing (`--fusion` / `always_fusion` / `[agent.fusion]`): a lead agent plans, delegates execution to a conservative sidekick via `fusion_delegate`, and reviews the result. Fusion remains off by default, delegation is lead-directed, and per-lane cost stats are reported on done events.
