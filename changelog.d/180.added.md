Added native Fusion dual-lane routing (`--fusion` / `always_fusion` / `[agent.fusion]`): a frontier lead agent delegates mechanical work to a cost-aware sidekick via `fusion_delegate`, with mid-session model switches at compaction boundaries and per-lane cost stats on done events.
