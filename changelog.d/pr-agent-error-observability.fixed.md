Tool failure logs now carry a classified error kind (`timeout`, `permission_denied`, `nonzero_exit`, …) instead of only a byte count, so a failure can be diagnosed from the log without the error text — which routinely holds captured command output or file contents — being persisted. A busy OpenAI response-chain session is now retried with backoff instead of failing the turn immediately.
