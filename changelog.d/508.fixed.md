Fixed abandoned queued prompt-slot collections still running their callbacks, abandoned state captures advancing revisions instead of being skipped and rechecked after draining, and session-log save retries looping forever against a stale cursor after a concurrent modification. Concurrent-modification saves now fail as a typed, logged error instead of silently retrying, preserving unpersisted-write accounting and lost-update protection.
