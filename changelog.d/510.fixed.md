Kept OpenAI prompt caching warm across a session: tool search no longer changes the tool list sent to the model, a slow plugin host no longer empties prompt slots, and subagents now share their parent's cache bucket. Tool search results now include each tool's input schema.
