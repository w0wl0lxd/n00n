Fixed remembered per-model thinking levels not applying at session start, kept rewound compaction checkpoints from being pruned while still referenced by the transcript, enforced the todo prompt byte cap across multiple oversized in-progress entries, and repaired the cursor model generator path after the crates/ move.
